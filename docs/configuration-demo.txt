Offline demo: temporary project and SQLite; no tracker, host, or setup calls.
Config results below are summarized by this demo; ordinary CLI results are JSON.

$ wayfinder config show --help
Wayfinder CLI — portable work orchestration for agents

wayfinder config show [--path FILE] [--json]
  Read project configuration and explicit personal choices without changing durable state.
  path: Project configuration file (default: wayfinder.toml in the current directory).
$ wayfinder completions bash --at "config show"
--path
--help
--json
$ wayfinder init --from reviewed.toml
model: demo-default-v1 (default)
runtime_mode: approval-required (required)
Second init: refused existing file; original preserved.
$ wayfinder config show
model: demo-default-v1 (default)
runtime_mode: approval-required (required)
init + show: no SQLite database created.
$ wayfinder config edit --set model demo-personal
model: demo-personal (personal)
runtime_mode: approval-required (required)
$ wayfinder config edit --editor demo-editor
model: demo-personal (personal)
runtime_mode: approval-required (required)
Project default changed to v2: source hash changed; personal choice and earlier resolution preserved.
$ wayfinder config edit --follow model
model: demo-default-v2 (default)
runtime_mode: approval-required (required)
Conflicting personal runtime_mode: rejected by project requirement.
Typed app.config.show.execute: demo-default-v2; project and database bytes unchanged.

Prompt construction only: fake ticket; T3 pending-host-preflight.
Configured setup: 1 step; not executed.
Loaded instructions: 1; content identity recorded.

--- Human-readable launch prompt ---
Tracker:
https://example.atlassian.net

Map:
jira:example:DEMO:map:DEMO-470

Ticket:
jira:example:DEMO:ticket:DEMO-491 — Inspect project configuration

Role:
task

Role guidance:
Implement only the selected task and report verification evidence.
This project's completion gates require acceptance verification and Jaren's human approval and merge of the implementation. Keep the ticket open while either gate is pending.
This demonstration authorizes no tracker mutations, map updates, setup execution, or agent launch.


Description:
Show resolved project defaults, requirements, and personal choices.

Acceptance criteria:
- Show configuration without changing durable state.
- Reject personal choices that conflict with project requirements.

Required output:
- Report the work performed and link any resulting artifacts.
- Report acceptance evidence, unverified criteria, and remaining blockers.

Authorization and completion:
This prompt does not grant authority to mutate tracker state, close tickets, or update maps. Follow the project/workflow instructions and explicit authorization for those actions. Passing acceptance checks alone does not authorize completion; all configured completion gates and required human approvals must also be satisfied.

Temporary demo project and SQLite removed.
